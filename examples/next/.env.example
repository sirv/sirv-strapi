# Copy to .env.local. The example fetches published Showcase entries from Strapi.
STRAPI_URL=http://localhost:1337
# Optional: only needed if the Showcase content type is NOT public-readable.
# The example Strapi app grants public read in src/index.ts bootstrap, so this can stay empty.
STRAPI_API_TOKEN=
