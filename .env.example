# Sirv API credentials — DEVELOPER USE ONLY for testing the Sirv API client
# against the live API. Production users authenticate inside Strapi admin via
# the email + password + OTP flow and never see these values.
#
# Copy this file to .env.local and fill in real values. .env.local is gitignored.

SIRV_CLIENT_ID=
SIRV_CLIENT_SECRET=
