import { Image } from '@strapi/icons';

/**
 * Sidebar / settings / custom-field icon. Uses a Design System icon as a placeholder;
 * milestone 7 swaps in the Sirv brand mark.
 */
export const PluginIcon = () => <Image />;

export default PluginIcon;
