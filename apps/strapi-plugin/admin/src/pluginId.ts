/**
 * The Strapi plugin id. Must match the `strapi.name` field in package.json and the
 * `plugin` namespace used server-side (`plugin::sirv.*`). Keep this in one place so the
 * menu link, settings section, custom field and translations all agree.
 */
export const PLUGIN_ID = 'sirv';
