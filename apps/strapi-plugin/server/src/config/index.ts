/**
 * Plugin config schema. Consumers may override under `config.plugins.sirv.config`.
 * Milestone 1 exposes no options; `validator` is a no-op.
 */
export default {
  default: {},
  validator() {},
};
