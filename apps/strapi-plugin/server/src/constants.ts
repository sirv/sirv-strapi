/** Plugin id - must match `strapi.name` in package.json and the admin `PLUGIN_ID`. */
export const PLUGIN_ID = 'sirv';

/** Key used in the plugin store: `strapi.store({ type: 'plugin', name: PLUGIN_ID })`. */
export const PLUGIN_STORE_KEY = 'settings';
